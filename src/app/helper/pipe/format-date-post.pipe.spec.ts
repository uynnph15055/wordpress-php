import { FormatDatePostPipe } from './format-date-post.pipe';

describe('FormatDatePostPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatDatePostPipe();
    expect(pipe).toBeTruthy();
  });
});
