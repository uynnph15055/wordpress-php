import { CountDatePipe } from './count-date.pipe';

describe('CountDatePipe', () => {
  it('create an instance', () => {
    const pipe = new CountDatePipe();
    expect(pipe).toBeTruthy();
  });
});
